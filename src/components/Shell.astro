---
// Shell component - Navigation and Footer
---

<div data-region="shell" class="d-none">
  <nav class="navbar navbar-dark navbar-expand-lg mb-4 no-print shell-navbar">
    <div class="container-fluid">
      <a class="navbar-brand d-flex align-items-center gap-2" href="#" id="brand-link">
        <i class="bi bi-leaf-fill" style="color: var(--color-primary);"></i>
        <div class="brand-copy d-flex flex-column">
          <span class="brand-title" id="brand-title">Pflanzenschutz</span>
          <span class="brand-tagline small" style="color: var(--color-text-muted);" id="brand-tagline"></span>
        </div>
      </a>
      
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <div class="collapse navbar-collapse" id="navbarNav">
        <!-- Infos button on the left -->
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <button class="btn btn-psm-primary-outline nav-btn" data-section="infos">
              <i class="bi bi-info-circle"></i> Infos
            </button>
          </li>
        </ul>
        
        <!-- Other buttons on the right -->
        <ul class="navbar-nav">
          <li class="nav-item">
            <button class="btn btn-psm-primary nav-btn" data-section="calc">
              <i class="bi bi-calculator"></i> Berechnung
            </button>
          </li>
          <li class="nav-item">
            <button class="btn btn-psm-secondary-outline nav-btn" data-section="settings">
              <i class="bi bi-gear"></i> Einstellungen
            </button>
          </li>
          <li class="nav-item">
            <button class="btn btn-psm-secondary-outline nav-btn" data-section="documentation">
              <i class="bi bi-journal-text"></i> Dokumentation
            </button>
          </li>
        </ul>
      </div>
    </div>
  </nav>
</div>

<footer data-region="footer" class="footer py-2 no-print d-none">
  <div class="container-fluid">
    <div class="row">
      <div class="col">
        <small style="color: var(--color-text-muted);">
          © 2025 Pflanzenschutzliste ·
          <a
            href="https://github.com/Abbas-Hoseiny/pestalozzi"
            target="_blank"
            rel="noopener noreferrer"
            class="text-reset text-decoration-underline d-inline-flex align-items-center gap-1"
          >
            <i class="bi bi-github" aria-hidden="true"></i>
            <span>GitHub</span>
          </a>
        </small>
      </div>
      <div class="col text-end">
        <small id="app-version" style="color: var(--color-text-muted);"></small>
        <button
          type="button"
          class="monitor-toggle"
          data-action="debug-overlay-toggle"
          title="Performance Monitor aktivieren"
        >
          <i class="bi bi-activity"></i>
        </button>
      </div>
    </div>
  </div>
</footer>

<style>
  .shell-navbar {
    position: sticky;
    top: 0;
    z-index: 1000;
    box-shadow: var(--shadow-md);
  }
  
  .nav-btn {
    margin: 0 0.25rem;
    border-radius: var(--radius-md);
    min-height: 40px;
  }
  
  .nav-btn.active {
    background-color: var(--color-primary);
    border-color: var(--color-primary);
    color: #fff;
  }
  
  .footer {
    position: fixed;
    bottom: 0;
    width: 100%;
    background-color: var(--footer-bg, var(--color-surface-1));
    box-shadow: var(--footer-shadow);
    z-index: 999;
  }
  
  .brand-logo {
    max-height: 40px;
    max-width: 40px;
  }

  .monitor-toggle {
    margin-left: 0.5rem;
    border: none;
    background: var(--color-surface-2);
    color: var(--color-text-muted);
    border-radius: var(--radius-full);
    padding: 0.15rem 0.5rem;
    font-size: 0.85rem;
    line-height: 1;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: background var(--transition-base), color var(--transition-base);
  }

  .monitor-toggle:hover,
  .monitor-toggle[data-active="true"] {
    background: var(--color-primary-subtle);
    color: var(--color-primary);
  }
  
  @media print {
    .no-print {
      display: none !important;
    }
  }
</style>
